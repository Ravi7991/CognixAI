import React from 'react';
import { Box, Typography } from '@mui/material';

const Header = () => {
  return (
    <Box sx={{ padding: 2, backgroundColor: '#050505' }}>
      <Typography variant="h6">Cognix AI - Header</Typography>
    </Box>
  );
};

export default Header;
