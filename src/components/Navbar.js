import React from 'react';
import { Box, Typography } from '@mui/material';

const Navbar = () => {
  return (
    <Box sx={{ padding: 2, backgroundColor: '#050505' }}>
      <Typography variant="h6">Navbar - Cognix AI</Typography>
    </Box>
  );
};

export default Navbar;
